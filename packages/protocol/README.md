# @ai-mmoprg/protocol

Shared TypeScript protocol types for client/server communication.

## Versioning

- `PROTOCOL_VERSION` is incremented for wire-incompatible changes.
- Additive changes (new optional fields/messages) can keep the same version.
