import { protocolVersion } from "@ai-mmoprg/protocol";

// Placeholder admin bootstrap; replace with dashboard app once requirements are finalized.
console.log("[admin] starting with protocol", protocolVersion);
