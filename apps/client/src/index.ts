import { protocolVersion } from "@ai-mmoprg/protocol";

// Placeholder client bootstrap; swap with browser app entrypoint once UI stack is selected.
console.log("[client] starting with protocol", protocolVersion);
